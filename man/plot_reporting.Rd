% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot_reporting}
\alias{plot_reporting}
\title{Plot estimated reporting completeness over time}
\usage{
plot_reporting(
  x,
  group_vars = c("region", "cause"),
  weight_var = "exposure",
  probs = c(0.1, 0.9),
  ribbon = TRUE,
  ...
)
}
\arguments{
\item{x}{A \code{vrcfit} object.}

\item{group_vars}{Character vector of columns to group by in addition to
\code{time}. Defaults to \code{c("region", "cause")}.}

\item{weight_var}{Column name used as weights when aggregating completeness across age/sex.
Defaults to \code{"exposure"}.}

\item{probs}{Quantiles to plot (must match those computed by \code{\link[=posterior_reporting]{posterior_reporting()}}).}

\item{ribbon}{Logical. If TRUE, draws a ribbon between the lowest and highest prob quantiles.}

\item{...}{Additional args passed to \code{\link[ggplot2:theme]{ggplot2::theme()}}.}
}
\value{
A ggplot object.
}
\description{
Convenience plotting helper for visualising estimated reporting completeness
over time, aggregated over age and sex.
}
