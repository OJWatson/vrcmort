% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conflict_effects.R
\name{vrc_conflict_effects}
\alias{vrc_conflict_effects}
\title{Region-specific conflict effects}
\usage{
vrc_conflict_effects(
  x,
  component = c("mortality", "reporting"),
  probs = c(0.1, 0.5, 0.9),
  draws = FALSE
)
}
\arguments{
\item{x}{A \code{vrcfit} object.}

\item{component}{Which model component to summarise: mortality (\code{beta_conf_rg})
or reporting (\code{gamma_conf_rg}).}

\item{probs}{Quantiles to report.}

\item{draws}{Logical. If \code{FALSE} (default), returns posterior summaries.
If \code{TRUE}, returns a long data.frame of posterior draws.}
}
\value{
A data.frame. If \code{draws = FALSE}, columns include \code{mean}, \code{sd} and the
requested quantiles. If \code{draws = TRUE}, columns include \code{draw} and \code{value}.
}
\description{
Extract posterior summaries of the conflict effect by region. This is most
useful when you fit a model with partial pooling of conflict effects (random
slopes by region), by setting \code{conflict = "region"} in \code{\link[=vrc_mortality]{vrc_mortality()}} and/or
\code{\link[=vrc_reporting]{vrc_reporting()}}.

The underlying Stan model always defines region-specific conflict effects
(\code{beta_conf_rg} for mortality and \code{gamma_conf_rg} for reporting). When the
corresponding feature is disabled (\code{conflict = "fixed"}), these region-specific
effects are equal to the global effect for all regions.
}
