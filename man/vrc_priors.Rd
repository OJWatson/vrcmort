% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/priors.R
\name{vrc_priors}
\alias{vrc_priors}
\title{Prior specification constructors}
\usage{
vrc_priors(
  beta_conf = normal(0.2, 0.3, autoscale = FALSE),
  beta_mort = normal(0, 0.3, autoscale = TRUE),
  gamma_conf = normal(0, 0.5, autoscale = FALSE),
  gamma_rep = normal(0, 0.5, autoscale = TRUE),
  kappa0 = NULL,
  kappa_post = normal(0, 0.7, autoscale = FALSE),
  alpha0 = normal(-9, 2, autoscale = FALSE),
  alpha_age = normal(0, 1, autoscale = FALSE),
  alpha_sex = normal(0, 0.5, autoscale = FALSE),
  sigma_u_lambda = normal(0, 0.5, autoscale = FALSE),
  sigma_v_lambda = normal(0, 0.2, autoscale = FALSE),
  sigma_beta_conf = normal(0, 0.3, autoscale = FALSE),
  sigma_v_lambda_region = normal(0, 0.2, autoscale = FALSE),
  sigma_u_rho = normal(0, 0.5, autoscale = FALSE),
  sigma_v_rho = normal(0, 0.2, autoscale = FALSE),
  sigma_gamma_conf = normal(0, 0.5, autoscale = FALSE),
  sigma_v_rho_region = normal(0, 0.2, autoscale = FALSE),
  delta_age_incr = normal(0, 0.5, autoscale = FALSE),
  delta_age_scale = normal(0, 1, autoscale = FALSE),
  phi = exponential(1),
  omega = beta(5, 2)
)
}
\arguments{
\item{beta_conf}{Prior for the conflict effect in the mortality component.
This parameter is constrained to be non-negative in the base model.}

\item{beta_mort}{Prior for additional mortality covariate effects.}

\item{gamma_conf}{Prior for the conflict effect in the reporting component.}

\item{gamma_rep}{Prior for additional reporting covariate effects.}

\item{kappa0}{Optional prior for baseline reporting on the logit scale.
If \code{NULL} (default), an anchored prior is used: cause 1 is centred on
logit(0.70), cause 2 on logit(0.90), and remaining causes on logit(0.85).}

\item{kappa_post}{Prior for the post-conflict reporting shift.}

\item{alpha0}{Prior for the mortality intercept.}

\item{alpha_age}{Prior scale for age effects in the mortality component.}

\item{alpha_sex}{Prior scale for sex effects in the mortality component.}

\item{sigma_u_lambda}{Prior scale for region random effect scales (mortality).}

\item{sigma_v_lambda}{Prior scale for national time RW scales (mortality).}

\item{sigma_beta_conf}{Prior scale for region-varying conflict slope scales (mortality).}

\item{sigma_v_lambda_region}{Prior scale for region-specific time RW scales (mortality).}

\item{sigma_u_rho}{Prior scale for region random effect scales (reporting).}

\item{sigma_v_rho}{Prior scale for national time RW scales (reporting).}

\item{sigma_gamma_conf}{Prior scale for region-varying conflict slope scales (reporting).}

\item{sigma_v_rho_region}{Prior scale for region-specific time RW scales (reporting).}

\item{delta_age_incr}{Prior scale for increments in the monotone age penalty.}

\item{delta_age_scale}{Prior scale for the overall monotone age penalty scale.}

\item{phi}{Prior for the NB2 dispersion parameter.}

\item{omega}{Prior for the labeling probability (MAR).}
}
\value{
A list of class \code{vrc_priors}.
}
\description{
\code{vrcmort} uses a lightweight prior specification system inspired by the
\code{rstanarm} and \code{epidemia} packages. Priors are represented as simple named
lists and are converted into Stan hyperparameters by \code{\link[=vrc_standata]{vrc_standata()}}.

Only a small set of distributions are currently supported because the
shipped Stan model uses conjugate-friendly priors:
\itemize{
\item \code{normal(location, scale)}
\item \code{exponential(rate)}
}

Create a bundled set of priors for use in \code{\link[=vrcm]{vrcm()}} / \code{\link[=vrc_fit]{vrc_fit()}}. The
returned object is converted into Stan hyperparameters by \code{\link[=vrc_standata]{vrc_standata()}}.

The defaults are designed to match the hard-coded priors used in earlier
versions of \code{vrcmort}.
}
