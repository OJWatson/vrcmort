% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stanmodels.R
\name{vrc_model_spec}
\alias{vrc_model_spec}
\title{Define a model specification}
\usage{
vrc_model_spec(
  model = "vr_reporting_model",
  stan_file = NULL,
  backend = c("rstan"),
  param_map = NULL
)
}
\arguments{
\item{model}{Either the name of a built-in Stan model (see
\code{\link[=vrc_model_names]{vrc_model_names()}}) or a path to a \code{.stan} file.}

\item{stan_file}{Optional explicit path to a \code{.stan} file. If supplied, it
overrides \code{model}.}

\item{backend}{Backend to use. Currently only \code{"rstan"} is supported.}

\item{param_map}{Named list mapping \code{rho}, \code{lambda}, \code{mu}, \code{y_rep}, and
optionally \code{log_lik} to Stan variable names.}
}
\value{
An object of class \code{vrc_model_spec}.
}
\description{
Create a small model specification object that can be used to fit either the
built-in Stan model shipped with \code{vrcmort} or a user-supplied Stan model
file. This keeps the R-side data preparation and posterior extraction tools
working even when users modify Stan code.

The \code{param_map} component is a named list mapping key quantities to Stan
variable names. The default mapping matches the generated quantities in the
base model.
}
